// Dev environment — points at the local ASP.NET Core API.
// (Override per-build with environment.prod.ts for production.)
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5177/api'
};
